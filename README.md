# News Headline Web Scraper

## 📌 Overview
This project is a simple Python-based web scraper that fetches top news headlines from a public website and saves them into a text file.

The script automates data collection by sending an HTTP request, parsing HTML content, and extracting headline text.

---

## 🚀 Features
- Fetches HTML content using HTTP GET requests
- Parses HTML using BeautifulSoup
- Extracts news headlines from `<h2>` tags
- Saves headlines into a `.txt` file
- Simple and easy-to-run CLI script

---

## 🛠️ Technologies Used
- Python 3
- requests
- BeautifulSoup (bs4)
- File handling

---

## ▶️ How to Run

### 1. Install dependencies
pip install requests beautifulsoup4

### 2. Run the script
python scrapper.py
